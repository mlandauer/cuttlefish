mutation($email: String!, $name: String!, $password: String, $currentPassword: String!) {
  updateAdmin(email: $email, name: $name, password: $password, currentPassword: $currentPassword) {
    errors {
      path
      type
      message
    }
  }
}
