mutation($name: String, $password: String!, $token: String!) {
  acceptAdminInvitation(name: $name, password: $password, token: $token) {
    admin {
      id
    }
    errors {
      message
      path
      type
    }
  }
}
