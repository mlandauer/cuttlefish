mutation($token: String!, $password: String!) {
  resetPasswordByToken(token: $token, password: $password) {
    admin {
      id
    }
    errors {
      message
      path
      type
    }
    token
  }
}
