# See http://help.github.com/ignore-files/ for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

# Ignore bundler config.
/.bundle

# Ignore all logfiles and tempfiles.
/log/*.log
/tmp

# Ignore .env.development so people can set their own env variables
.env.development

cuttlefish.sublime-workspace
db/emails/development
db/emails/test
db/archive
config/newrelic.yml
coverage
.vagrant

provisioning/roles/newrelic.newrelic-infra
provisioning/roles/rvm.ruby
provisioning/roles/geerlingguy.certbot
provisioning/roles/DavidWittman.redis
provisioning/roles/ANXS.postgresql
provisioning/roles/abtris.nginx-passenger
provisioning/roles/nickhammond.logrotate

postfix_log/supervisor/*.log
postfix_log/maillog
postfix_log/messages

# Use ansible as a virtualenv if you want
ansible
