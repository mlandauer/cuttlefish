%table.table.table-condensed
  %thead
    %tr
      %th Time
      %th Link
      %th Client
      %th OS
      %th IP
  %tbody
    - delivery.click_events.each do |event|
      %tr
        %td
          = time_ago_in_words(event.created_at)
          ago
        %td= link_to event.url
        %td
          = event.calculate_ua_family
          - if event.calculate_ua_version
            %span.muted (#{event.calculate_ua_version})
        %td
          = event.calculate_os_family
          - if event.calculate_os_version
            %span.muted (#{event.calculate_os_version})
        %td= event.ip
