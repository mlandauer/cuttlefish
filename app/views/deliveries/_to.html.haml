

= paginated_section deliveries, renderer: PagerRenderer, previous_label: image_tag("pager/previous.png"), next_label: image_tag("pager/next.png"), text: "Email" do
  
  %p.count= page_entries_info deliveries, model: "email"
  %table#deliveries.table.table-striped
    %tbody(data-provides="rowlink")
      - deliveries.each do |delivery|
        %tr
          %td.description
            = link_to delivery.to, delivery, class: "rowlink"
            %br
            = delivery.subject
          - if App.normal_apps?
            %td.app.hidden-phone= delivery.app_name
          %td.time-and-labels
            = time_ago_in_words(delivery.created_at)
            ago
            %br
            = delivered_label(delivery.status)
            - if delivery.opened?
              %span.label.label-success Opened
            - if delivery.clicked?
              %span.label.label-success Clicked
