.page-header
  %h1 Edit my details

= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class: "form-horizontal" }) do |f|
  = devise_error_messages!

  %fieldset
    .control-group
      = f.label :gravatar, class: "control-label"
      .controls
        = gravatar_image_tag(@admin.email, gravatar: {size: 35, secure: true, default: :identicon}, class: "img-circle")
        %span.help-inline
          = link_to "Change your gravatar", "http://gravatar.com/"

    .control-group
      = f.label :name, class: "control-label"
      .controls
        = f.text_field :name, placeholder: "Jane Smith"
        %span.help-block
          The name that other people will see

    .control-group
      = f.label :email, class: "control-label"
      .controls
        = f.email_field :email

    .control-group
      = f.label :password, "New password", class: "control-label"
      .controls
        = f.password_field :password
        %span.help-block
          Leave blank if you don't want to change it

    .control-group
      = f.label :current_password, class: "control-label"
      .controls
        = f.password_field :current_password
        %span.help-block
          We need your current password to confirm your changes

  .control-group
    .controls
      = f.submit "Update", class: "btn btn-primary"

%h3 Cancel my account

%p
  Unhappy?
  = button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: "btn btn-danger"
